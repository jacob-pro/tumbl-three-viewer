events {
}
http {
    include mime.types;
    server {
        listen 80;
        server_name tumblr;
        location /blogs {
            alias /blogs/;
            autoindex on;
        }
        location / {
            alias /src/;
        }
    }
}
